import 'package:flutter/material.dart';

class BuildTableRow extends StatelessWidget {
  const BuildTableRow({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
